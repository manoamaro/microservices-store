apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgresql.fullname" . }}-secret
type: Opaque
data:
  POSTGRES_USER: {{ .Values.auth.rootUsername }}
  POSTGRES_PASSWORD: {{ .Values.auth.rootPassword }}
